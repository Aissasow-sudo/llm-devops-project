# Screenshots

- [x] `floci-ui.png` — Floci UI, Console Home sur GCP, "Runtime reachable" sur `http://floci-gcp:4588` (étapes 3 et 4).
- [x] `floci.png` — terminal montrant `docker ps` (les 5 conteneurs Up) et `curl -i http://localhost:4588` (étape 3).
- [x] `resources-storage.png` — Cloud Explorer → Storage, bucket `cloud-project-dev-bucket` créé par `terraform apply` (étape 11).
- [x] `resources-pubsub.png` — Cloud Explorer → Pub/Sub, topic `cloud-project-dev-topic` créé par `terraform apply` (étape 11).
- [x] `destroy.png` — Floci UI après `terraform destroy`, Pub/Sub à 0 ressources (étape 12).
